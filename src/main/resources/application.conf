ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }

    application {
        modules = [ dev.rivu.rivutalks.ApplicationKt.module ]
    }
}

key {
    secret = ANY_RANDOM_SECRET_VALUE
}

database {
    host = ${PGHOST}
    port = ${PGPORT}
    name = ${PGDATABASE}
    user = ${PGUSER}
    password = ${PGPASSWORD}
    driver = ${DATABASE_DRIVER}
    maxPoolSize = 10
}